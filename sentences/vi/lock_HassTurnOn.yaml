language: vi
intents:
  HassTurnOn:
    data:
      # - sentences:
      #     - "khoá <name> [trong <area>]"
      #   requires_context:
      #     domain: lock
      #   response: lock

      - sentences:
          - "khoá [tất cả] [các] khóa [trong|tại] <area>"
          - "khoá [tất cả] <area> [(ổ khoá|cửa|khóa)]"
        slots:
          domain: "lock"
          name: "all"
        response: lock
