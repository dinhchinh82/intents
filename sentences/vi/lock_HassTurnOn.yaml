language: vi

intents:
  HassTurnOn:
    data:
      - sentences:
          - "<khóa> <name> [ [<trong>] <area>] "
        requires_context:
          domain: lock
        response: lock

      - sentences:
          - "<khóa> [tất cả|mọi] [cánh] <cửa> <trong_area>"
            # - "khoá [tất cả] [các] [trong|tại] <area>"
            # - "khoá [tất cả] <area> [(ổ khoá|cửa|khóa)]"
        expansion_rules:
          cửa: "khóa|cửa"
        slots:
          domain: "lock"
          name: "all"
        response: lock
